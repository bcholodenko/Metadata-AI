# Metadata-AI — Python dependencies
# https://github.com/bcholodenko/Metadata-AI
#
# Install with:
#   pip install -r requirements.txt
#
# Optional extras are listed at the bottom.

# ── Core ─────────────────────────────────────────────────────────────────────

# OpenAI-compatible client — used to talk to LM Studio's local API endpoint
openai>=1.30.0

# Image loading and processing
Pillow>=10.0.0

# HEIC / HEIF image support (Apple Live Photos, iPhone exports)
pillow-heif>=0.13.0

# IPTC metadata reading (existing keyword extraction)
iptcinfo3>=2.1.4

# Natural sort for filename ordering (e.g. img2 before img10)
natsort>=8.4.0

# Rich terminal output: progress bars, panels, tables
rich>=13.7.0

# ── Optional — install for additional features ────────────────────────────────

# Geotagging via Nominatim geocoding (--geotag flag)
# geopy>=2.4.0

# RAW camera file support (CR2, NEF, ARW, RAF, …)
# rawpy>=0.19.0
# numpy>=1.24.0        # rawpy dependency

# Interactive arrow-key menus (falls back to numbered lists if absent)
# questionary>=2.0.0

# OCR text extraction from photo backs (Tesseract wrapper)
# pytesseract>=0.3.10
# Tesseract binary also required: https://github.com/tesseract-ocr/tesseract

# ── External binaries (not pip-installable) ───────────────────────────────────
#
# ExifTool  — writes metadata into JPEG/TIFF/PNG/HEIC/WebP files
#   macOS:   brew install exiftool
#   Ubuntu:  sudo apt install libimage-exiftool-perl
#   Windows: https://exiftool.org
#
# ffmpeg    — required for video frame extraction and metadata writing
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
#   Windows: https://ffmpeg.org/download.html
